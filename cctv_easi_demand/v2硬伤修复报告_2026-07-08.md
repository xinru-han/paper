# v2 硬伤清单修复报告（2026-07-08）

**对照文件**：`v2代码计量审查_硬伤清单.md`。逐条状态如下。

## 0. "v2 从未成功运行" —— 审查对象是过期快照

审查的 zip 只含 30/31/32 三个脚本和一份崩溃日志。当前仓库版本领先于该快照：A1/A2 修复后管线已全量跑通（`outputs/_PIPELINE_DONE`，脚本 30–39），并新增了清单缺席的 34（配对bootstrap）、35（福利CV）、36–38（曲率约束与正则性）、39（描述统计）。

## A. 阻断性 bug —— 快照后已修，本轮补断言

| # | 状态 |
|---|---|
| A1 | ✅ 已修（重复merge已删，`_log_30_fix.txt` 跑通） |
| A2 | ✅ 已修（`difftime(Sys.time(), t0, units="mins")`） |
| 断言 | ✅ 本轮新增：30 与 32 各加 `stopifnot(!anyNA(income_group_order))` |

## B. 计量硬伤 —— 本轮全部修复

**B1 混合价 fold 泄漏 ✅**
- `int_group_fold` 按 `(fold, 省, 月, 组)` 聚合（不再对fold取平均）；`gap_p` 对齐常数、混合价 `lp_hybrid` 全部 fold 专属；panel 按 `(fold,省,月,组)` 合并。
- 认证表继续用 fold 平均版（认证不要求自我排除）。
- 验证：671 个混合格子的价格 100% 随 fold 变化；抽查 20 万行 panel，家庭拿到的混合价与其 fold 专属价逐行一致。
- 影响范围：仅 R2_hybrid 稳健性规格；主规格纯外部价不经过此路径。

**B2 两步不确定性 ✅（快照后已实现）**
- `34_bootstrap_v2.R` + `34b` 合并脚本已存在：家庭配对聚类bootstrap，频数权重免复制行，每轮完整重跑 probit→Stone权重→曲率约束FGLS→参与边际弹性，**B=210**。
- 主表全部 CI 来自该 bootstrap；解析 SE 保留为参考并在论文 3.3 节明确标注 two-step-naive（视 Φ̂、φ̂、Σ̂、Stone权重为已知）。

**B3 wild cluster 名不副实 + 未学生化 ✅（学生化+改名双管齐下）**
- 重写为 `score_multiplier_boot`：每个 draw 用 draw 专属省聚类 CR1 SE 学生化，比较 t 统计量分布（利用 score 结构，每 draw 仅 24×P 重加权，成本极低）。
- `system_cluster_vcov` 加 G/(G−1) CR1 小样本因子。
- 输出文件改名 `score_multiplier_boot_province_price_terms_v2.csv`；论文明确写 "studentized score-based cluster multiplier bootstrap (Webb weights)，非 null-imposed WCR"。

**B4 QUAIDS 错误标签 ✅**
- 代码参数 `quaids` → `y2p`（`build_Ag` 保留旧 rds 兼容）；输出改为 `expenditure_elasticity_easi_y2p_v2.csv`、`own_price_easi_vs_y2p_v2.csv`（列 `own_easi_y2p`）；rds 字段 `elasticities_y2p`。
- 论文表述："An EASI variant that replaces y × price with y² × price interactions—…not a QUAIDS system"。

## C. 中度问题 —— 全部处理

**C1 价格–份额覆盖不匹配 ✅**
- 新增 `outputs/price/price_coverage_2021_v2.csv`：7 组 100%、主食 90.1%、**乳制品 62.2% 最低**（酸奶/奶酪/黄油无外部序列，与审查预测一致）。
- 新增 **R9_priced_subbasket** 稳健性：份额收窄到被定价子篮重估全管线。结果：9/9 自价格弹性为负，乳制品 −1.11（主规格无约束 −0.98），覆盖不匹配不驱动结果。修复后的 R2_hybrid（fold 专属）同样 9/9 为负。
- 论文 2.2 节披露覆盖率并指向第 5 节验证。

**C2 认证 pooled 截面问题 ✅**
- 认证表新增第三指标 `corr_within_prov_median`（省内去均值后时序相关的中位数）。

**C3 截断+再归一披露 ✅**
- `residual_share_G03_distribution_v2.csv`：残差组预测份额 <0.5% 家庭-月为负（最小 −0.38）。论文 3.1 节明确披露：加总仅对潜在份额按构造成立，尾部截断+再归一。

**C4 OOF 三处不洁 ✅（修掉而非仅披露）**
- 折外拟合现在完全 fold-clean：Stone 权重、y、相对价、Mundlak 均值、probit、Σ（FGLS重迭代）全部只用训练折。`sw_k` 死参数问题消除（预测用训练折权重推导的变量）。
- 新 OOF R²：0.020–0.064，9 组全正（低于旧版是诚实化的代价，论文表述"positive for every equation"仍成立）。

**C5 A(z) 推断链 ✅**
- 发现并披露：省聚类 joint Wald 在此秩不可行（108 约束 > 24 簇秩上限）。
- 解法：家庭聚类 Wald（678, df=108, p<1e-83）+ **省级学生化 score bootstrap sup-t 联合检验（sup-t=5.57, p<0.001）**，双重确认；论文注明最终显著性以配对 bootstrap 为准。

**C6 测量误差方向 ✅**
- 论文第 5 节新增三角论证段落：外部价经典测量误差→弹性衰减偏向0；与 fold 排除混合价同号同量级、与 v1 单位价值污染估计（G02/G06/G09 正号）方向相反 ⇒ 结果对测量误差不敏感。

**C7 参与边际口径 ✅**
- 论文 3.3 节明确：弹性含参与边际（probit 随价格扰动重算）；Mundlak 均值在扰动中固定 = 短期响应、家庭长期成分不变。

## 验证与一致性

- **主规格估计不受任何修改影响**：新旧全量无约束主估计最大差异 ~2e-10（对照未重跑的 36 旧产物）。因此 B=210 bootstrap、曲率约束结果、福利 CV 全部继续有效，34/35/36 无需重跑。
- 途中修掉一个本轮引入的 bug：`spend_month_priced` 未在 panel 填零导致 R9 只剩全购买家庭（xtx 奇异）；30 的填零列表与 33 的防御填零均已加。

## 论文同步（build_paper.py）

摘要、3.1/3.3、2.2、4.3、5 节、参考表述全部同步：学生化 score bootstrap 命名、EASI-y2p、覆盖率披露、三指标认证、截断披露、sup-t、三角论证、fold 专属混合价说明、R9 列入稳健性列表（表 6 自动纳入新列）。

## E 清单（既定缺口，非本轮范围）

零消费双机制排除变量、4 周频率、winsor 多版本、年度 benchmark、封控事件验证等仍按上轮方案排期；其中购买周期排除变量建议下一轮优先。
